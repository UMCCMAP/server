/*package com.umc.cmap.domain.cmap.service;

import com.umc.cmap.domain.cafe.repository.CafeRepository;

@Service
public class CMAPService {
    private final CafeRepository cafeRepository;
    private final CafeFilterRepository cafeFilterRepository;
    private final FilterRepository filterRepository;

    // 생성자 주입

    public List<Cafe> searchCafesByName(String cafeName) {
        // cafeName을 사용하여 카페를 검색하는 로직 구현
    }

    public List<Cafe> searchCafesByTheme(String theme) {
        // theme을 사용하여 카페를 필터링하는 로직 구현
    }

    public List<Cafe> getRandomCafes() {
        // 랜덤으로 카페를 선택하여 반환하는 로직 구현
    }

    public List<Filter> getAllFilters() {
        // 모든 필터를 조회하는 로직 구현
    }

    public Filter createFilter(Filter filter) {
        // 필터 생성 로직 구현
    }

    public Filter updateFilter(Long filterIdx, Filter updatedFilter) {
        // 특정 필터를 업데이트하는 로직 구현
    }

    public void deleteFilter(Long filterIdx) {
        // 특정 필터를 삭제하는 로직 구현
    }
}
*/